export const SwapDirections = Object.freeze({
    UP: "up",
    DOWN: "down",
    LEFT: "left",
    RIGHT: "right"
});

export const GameState = Object.freeze({
    WIN: "win",
    LOSE: "lose",
    PLAYING: "play"
})